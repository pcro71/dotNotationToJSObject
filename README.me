export data from export tab of the masterquestions file into input_all.csv
save file
run master.js
it should cycle through every row, copying it to input.js, where it is cleaned up and pasted to masterDialog.js